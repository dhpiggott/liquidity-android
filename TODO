Update licences.xml
Update screenshots
Review Lint warnings
Fix connection bug where client keeps socket open (repro: join an invalid game), possibly also intermittently causes connection failures that resolves with process restart
Re-enable resource shrinking
