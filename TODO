* Review Fragments API usage and keystore format now that ICS is dropped
* Update screenshots
* Fix connection bug where client keeps socket open (repro: join an invalid game), possibly also intermittently causes
  connection failures that resolves with process restart
